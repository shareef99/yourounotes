import React from "react";

interface Props {}

const Footer = (props: Props) => {
    return (
        <footer className="h-screen">
            <h1>DAsd</h1>
        </footer>
    );
};

export default Footer;
